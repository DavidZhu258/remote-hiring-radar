# Gold Data: remote-hiring-radar

`seed_gold.jsonl` contains 3 starter cases to lock schema and rubric shape. `full_gold.jsonl` is intentionally empty until real reviewed cases are added.

Rules: keep frozen cases versioned, include hard negatives, and record annotation decisions in `annotation_template.csv`.
